# === DEFAULT ===
[default.bot]
parse_mode = 'HTML'

# logs
[default.logs]
log_level = 'DEBUG'

[default.logs.config_dict]
version = 1
disable_existing_loggers = false

[default.logs.config_dict.formatters.formatter]
format = '[{asctime}] #{levelname:<8} {filename:<17}:{lineno:4} - <{funcName}> - {message}'
datefmt = '%Y.%m.%d %H:%M:%S'
style = '{'

[default.logs.config_dict.handlers.stdout]
class = 'logging.StreamHandler'
formatter = 'formatter'
stream = 'ext://sys.stdout'

[default.logs.config_dict.root]
handlers = ['stdout']

# redis
[default.redis]
port = 6379

# postgres
[default.postgres]
name = 'mentor_db'
port = 5432


# === PRODUCTION ===
# logs
[production.logs]
log_level = 'INFO'

[production.logs.config_dict.handlers.file]
class = 'logging.handlers.RotatingFileHandler'
formatter = 'formatter'
filename = '/app/logs/app.log'
maxBytes = 10485760
backupCount = 3
mode = 'a'

[production.logs.config_dict.root]
handlers = ['stdout', 'file']
